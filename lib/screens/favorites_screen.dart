import 'package:flutter/material.dart';

class FavoiritesScreen extends StatelessWidget {
  const FavoiritesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Favories"),
    );
  }
}
